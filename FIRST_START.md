# 🚀 Инструкция по первому запуску

## Шаг 1: Настройка Firebase Firestore

### 1.1. Создайте базу данных Firestore

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект `calendar-b87ed`
3. В меню слева нажмите **"Firestore Database"**
4. Если база данных еще не создана:
   - Нажмите **"Создать базу данных"** (Create database)
   - Выберите **"Начать в тестовом режиме"** (Start in test mode)
   - Выберите регион (например, `europe-west1` или ближайший к вам)
   - Нажмите **"Включить"** (Enable)

### 1.2. Настройте правила безопасности

1. В разделе **Firestore Database** перейдите на вкладку **"Правила"** (Rules)
2. Замените правила на следующие:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /calendars/{calendarId} {
      allow read, write: if true;
    }
  }
}
```

3. Нажмите **"Опубликовать"** (Publish)

> ⚠️ **Важно**: Эти правила позволяют читать и писать данные любому, у кого есть ссылка с `calendarId`. Для личного использования это нормально.

## Шаг 2: Запуск приложения

### Вариант 1: Простой запуск (для тестирования)

1. Откройте файл `index.html` в браузере:
   - **Windows**: Двойной клик по файлу или правый клик → "Открыть с помощью" → выберите браузер
   - **Mac**: Двойной клик по файлу или правый клик → "Открыть с помощью" → выберите браузер
   - **Linux**: `xdg-open index.html` или откройте через браузер

2. Приложение откроется в браузере

### Вариант 2: Запуск через локальный сервер (рекомендуется)

Для корректной работы Firebase рекомендуется использовать локальный веб-сервер:

#### Через Python (если установлен):

```bash
# Python 3
python3 -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000
```

Затем откройте в браузере: `http://localhost:8000`

#### Через Node.js (если установлен):

```bash
# Установите http-server глобально (один раз)
npm install -g http-server

# Запустите сервер
http-server -p 8000
```

Затем откройте в браузере: `http://localhost:8000`

#### Через VS Code (если используете):

1. Установите расширение "Live Server"
2. Правый клик по `index.html` → "Open with Live Server"

## Шаг 3: Первое использование

1. **При первом открытии:**
   - Приложение автоматически создаст уникальный `calendarId`
   - ID появится в URL (например: `?c=abc123xyz`)
   - В шапке приложения появится информация о календаре и кнопка "Поделиться"

2. **Разрешите уведомления:**
   - Браузер запросит разрешение на показ уведомлений
   - Нажмите **"Разрешить"** для работы напоминаний

3. **Добавьте первую задачу:**
   - Нажмите кнопку **"+ Добавить"** в любом разделе
   - Заполните форму и сохраните
   - Данные автоматически сохранятся в Firebase

4. **Поделитесь календарем:**
   - Нажмите кнопку **"🔗 Поделиться"**
   - Ссылка скопируется в буфер обмена
   - Отправьте ссылку тем, с кем хотите поделиться календарем

## Шаг 4: Проверка работы

### Проверьте консоль браузера:

1. Откройте консоль разработчика:
   - **Chrome/Edge**: `F12` или `Ctrl+Shift+I` (Windows) / `Cmd+Option+I` (Mac)
   - **Firefox**: `F12` или `Ctrl+Shift+K` (Windows) / `Cmd+Option+K` (Mac)
   - **Safari**: `Cmd+Option+I` (нужно включить меню разработчика в настройках)

2. Проверьте наличие ошибок:
   - Если видите ошибки подключения к Firebase - проверьте настройки в `firebase-config.js`
   - Если видите ошибки правил Firestore - проверьте правила безопасности

### Проверьте Firebase Console:

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Перейдите в **Firestore Database** → **Данные** (Data)
3. После добавления задачи должна появиться коллекция `calendars` с документом, содержащим ваш `calendarId`

## 🐛 Решение проблем

### Ошибка: "Firebase: Error (auth/unauthorized-domain)"
- **Решение**: В Firebase Console → Настройки проекта → Ваши приложения → Добавьте домен в "Авторизованные домены"
- Для локального тестирования добавьте `localhost`

### Ошибка: "Firebase: Missing or insufficient permissions"
- **Решение**: Проверьте правила Firestore (см. Шаг 1.2)

### Данные не сохраняются
- Проверьте консоль браузера на наличие ошибок
- Убедитесь, что Firestore Database создана и включена
- Проверьте подключение к интернету

### Не работает кнопка "Поделиться"
- Используйте современный браузер (Chrome, Firefox, Safari, Edge)
- Или скопируйте ссылку из адресной строки вручную

## ✅ Готово!

После выполнения всех шагов приложение должно работать:
- ✅ Данные сохраняются в Firebase
- ✅ Синхронизация между устройствами работает
- ✅ Можно делиться календарем по ссылке
- ✅ Напоминания работают (если разрешены уведомления)

