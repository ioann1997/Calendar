# 📅 Трекер ритуалов и задач

Веб-приложение для отслеживания ежедневных ритуалов, задач от хозяина и еженедельных ритуалов с системой напоминаний и **синхронизацией через Firebase**.

## ✨ Функции

- **Ежедневные ритуалы** - отслеживание ежедневных задач с возможностью установки времени напоминания
- **Задачи от хозяина** - специальный раздел для важных задач
- **Еженедельные ритуалы** - задачи, которые повторяются каждую неделю в определенный день
- **Напоминания** - уведомления в браузере в установленное время
- **🔥 Синхронизация через Firebase** - все данные хранятся в облаке и синхронизируются между устройствами в реальном времени
- **Общий доступ** - поделитесь ссылкой, и все смогут видеть и редактировать один календарь
- **Локальный кэш** - данные также сохраняются локально для работы оффлайн
- **Автоматический сброс** - еженедельные задачи автоматически сбрасываются в начале новой недели

## 🚀 Настройка Firebase

### Шаг 1: Создание проекта Firebase

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Нажмите "Добавить проект" (или "Add project")
3. Введите название проекта (например, "Calendar Tracker")
4. Отключите Google Analytics (не обязательно) или оставьте включенным
5. Нажмите "Создать проект"

### Шаг 2: Настройка Firestore Database

1. В меню слева выберите **"Firestore Database"**
2. Нажмите **"Создать базу данных"** (или "Create database")
3. Выберите режим **"Начать в тестовом режиме"** (Start in test mode) - для личного использования это безопасно
4. Выберите регион (например, `europe-west` или ближайший к вам)
5. Нажмите **"Включить"** (Enable)

### Шаг 3: Получение конфигурации

1. В меню слева выберите **⚙️ Настройки проекта** (Project settings)
2. Прокрутите вниз до раздела **"Ваши приложения"** (Your apps)
3. Нажмите на иконку **`</>`** (Web)
4. Зарегистрируйте приложение (введите любое имя, например "Calendar Web")
5. Скопируйте объект конфигурации `firebaseConfig`

### Шаг 4: Настройка правил безопасности Firestore

1. Перейдите в **Firestore Database** → **Правила** (Rules)
2. Замените правила на следующие (для доступа по ссылке):

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /calendars/{calendarId} {
      allow read, write: if true; // Доступ для всех, у кого есть ссылка
    }
  }
}
```

3. Нажмите **"Опубликовать"** (Publish)

> ⚠️ **Важно**: Эти правила позволяют читать и писать данные любому, у кого есть ссылка с `calendarId`. Для личного использования это нормально, но если нужна дополнительная защита, можно добавить проверку токенов.

### Шаг 5: Подключение к приложению

1. Откройте файл `firebase-config.js` в проекте
2. Замените значения в объекте `firebaseConfig` на ваши из Firebase Console:

```javascript
const firebaseConfig = {
    apiKey: "ВАШ_API_KEY",
    authDomain: "ВАШ_PROJECT_ID.firebaseapp.com",
    projectId: "ВАШ_PROJECT_ID",
    storageBucket: "ВАШ_PROJECT_ID.appspot.com",
    messagingSenderId: "ВАШ_MESSAGING_SENDER_ID",
    appId: "ВАШ_APP_ID"
};
```

3. Сохраните файл

## 📖 Использование

### Первый запуск

1. Откройте файл `index.html` в современном браузере
2. Приложение автоматически создаст уникальный `calendarId` и добавит его в URL
3. При первом использовании браузер может запросить разрешение на показ уведомлений - разрешите для работы напоминаний

### Поделиться календарем

1. Нажмите кнопку **"🔗 Поделиться"** в шапке приложения
2. Ссылка будет скопирована в буфер обмена
3. Отправьте ссылку тем, с кем хотите поделиться календарем
4. Все, у кого есть ссылка, увидят один и тот же календарь и смогут его редактировать

### Работа с задачами

1. Добавляйте задачи через кнопку **"+ Добавить"** в каждом разделе
2. Отмечайте выполненные задачи галочкой
3. Редактируйте или удаляйте задачи через соответствующие кнопки
4. Все изменения автоматически синхронизируются со всеми устройствами

## 🔔 Напоминания

Для работы напоминаний:
1. При добавлении задачи включите опцию **"Включить напоминание"**
2. Установите время напоминания
3. Для еженедельных ритуалов выберите также день недели
4. Разрешите браузеру показывать уведомления

Напоминания будут показываться в установленное время каждый день (для ежедневных) или в определенный день недели (для еженедельных).

## 💾 Хранение данных

- **Основное хранилище**: Firebase Firestore (облако)
- **Локальный кэш**: localStorage (для быстрой загрузки и работы оффлайн)
- **Синхронизация**: изменения синхронизируются в реальном времени между всеми устройствами

## 🛠️ Технологии

- HTML5
- CSS3 (с градиентами и анимациями)
- Vanilla JavaScript
- Firebase Firestore (для синхронизации)

## 📝 Примечания

- Для работы напоминаний необходим современный браузер с поддержкой Web Notifications API
- Рекомендуется использовать Chrome, Firefox, Safari или Edge
- Напоминания работают только когда браузер открыт (или в фоновом режиме)
- Firebase бесплатный план (Spark) включает:
  - 50,000 чтений в день
  - 20,000 записей в день
  - 20,000 удалений в день
  - 1 GB хранилища
  
  Для личного календаря этого более чем достаточно!

## 🔒 Безопасность

- Каждый календарь имеет уникальный ID в URL
- Доступ к календарю имеют только те, у кого есть ссылка
- Данные хранятся в Firebase и защищены правилами Firestore
- Для дополнительной защиты можно настроить аутентификацию (требует дополнительной настройки)

## 🆘 Решение проблем

### Ошибка подключения к Firebase
- Проверьте, что вы правильно скопировали конфигурацию в `firebase-config.js`
- Убедитесь, что Firestore Database создана и включена
- Проверьте правила безопасности Firestore

### Данные не синхронизируются
- Проверьте подключение к интернету
- Откройте консоль браузера (F12) и проверьте наличие ошибок
- Убедитесь, что Firebase проект активен

### Не работает кнопка "Поделиться"
- Используйте современный браузер с поддержкой Clipboard API
- Или скопируйте ссылку из адресной строки вручную
